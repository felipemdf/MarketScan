export default interface AppConfig {
  nodeEnv: string;
  logLevel: string;
  timezone: string;
  // tempDir: string;
  // s3Bucket?: string;
  // maxRetries: number;
  // retryDelay: number;
  // rateLimitDelay: number;
  // enableMetrics: boolean;
  // sentryDsn?: string;
}
